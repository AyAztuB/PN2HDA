# PN2HDA

## Dependencies

- [libxml2](https://gitlab.gnome.org/GNOME/libxml2)
- CMake (>= 3.21.2)
- C (std C99) -- GCC (or CLANG)

## Build

```sh
cmake -B build
cmake --build build --target pn2hda
```
